# Copia este archivo a  .env  y ajusta los valores.
# IMPORTANTE: DB_NAME / DB_USER / DB_PASS deben coincidir con lo que espera tu conexion.php
# y con el dump que pongas en ./_dbinit/.

DB_NAME=bd_op
DB_USER=siapc
DB_PASS=siapc
DB_ROOT_PASS=root
